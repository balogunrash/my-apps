apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myhello.fullname" . }}-test-connection"
  labels:
{{ include "myhello.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "myhello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
